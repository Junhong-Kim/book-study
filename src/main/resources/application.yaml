spring:
  datasource:
    hikari:
      jdbc-url: jdbc:mysql://localhost:3306/book_study?useSSL=false&characterEncoding=UTF-8&serverTimezone=UTC
      driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    properties:
      hibernate:
        show_sql: true
        format_sql: true
        use_sql_comments: true

logging:
  level:
    org:
      hibernate:
        SQL: debug
        type: trace
